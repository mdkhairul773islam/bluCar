import Panel from "@/components/common/Panel";

export const metadata = {
  title: "Purchase",
};

const PurchasePage = () => {
  return (
    <>
      <Panel title="All Purchase"></Panel>
    </>
  );
};

export default PurchasePage;
